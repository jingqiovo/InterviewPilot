# InterviewPilot 服务器部署指南

> 从零开始在全新服务器上部署 InterviewPilot，支持域名 + HTTPS 访问

---

## 环境说明

| 项目 | 说明 |
|------|------|
| 系统 | Ubuntu 20.04 / 22.04 LTS（CentOS 也适用，命令略有不同） |
| Web 服务器 | Nginx |
| 域名 | 需要已解析到服务器 IP |
| SSL 证书 | Certbot（Let's Encrypt）自动申请 |
| 项目类型 | 纯静态前端（Vite build 产物） |

---

## 第一步：连接服务器

```bash
# 使用 SSH 连接服务器
ssh root@你的服务器IP
```

> 首次连接需要输入密码或上传 SSH 公钥
> 如果是云服务器（阿里云/腾讯云），在控制台先开放 `22` 端口

---

## 第二步：更新系统

```bash
apt update && apt upgrade -y
```

---

## 第三步：安装基础工具

```bash
apt install -y curl wget git unzip zip
```

---

## 第四步：安装 Nginx

```bash
# 安装 nginx
apt install -y nginx

# 启动 nginx
systemctl start nginx

# 设置开机自启
systemctl enable nginx

# 检查状态
systemctl status nginx
```

确认输出中显示 `active (running)` 即成功。

---

## 第五步：开放防火墙端口

```bash
# 查看防火墙状态
ufw status

# 开放 HTTP 和 HTTPS 端口
ufw allow 22/tcp    # SSH
ufw allow 80/tcp     # HTTP
ufw allow 443/tcp    # HTTPS

# 启用防火墙（如果提示确认，输入 y）
ufw enable
```

> 如果是阿里云/腾讯云等国内云服务器，还需要**在控制台安全组里手动开放** 80 和 443 端口！

---

## 第六步：在本地打包项目

在你的 Mac 上运行：

```bash
# 进入项目目录
cd "/Users/imac/ui:ux"

# 安装依赖（如还未安装）
npm install

# 打包生产版本
npm run build
```

打包完成后，项目根目录会出现一个 `dist/` 文件夹，里面就是需要上传到服务器的内容。

---

## 第七步：上传到服务器

### 方法一：scp 命令（推荐）

```bash
# 在 Mac 本地项目目录执行
cd "/Users/imac/ui:ux"
scp -r dist/* root@你的服务器IP:/var/www/interviewpilot/
```

> 首次连接会提示确认，输入 `yes` 回车，然后输入服务器密码

### 方法二：如果提示权限问题，先在服务器创建目录

```bash
# 在服务器上执行
mkdir -p /var/www/interviewpilot
chmod -R 755 /var/www/interviewpilot
```

然后重新执行 scp 命令。

### 方法三：使用 rsync（增量同步，更快）

```bash
rsync -avz --delete dist/ root@你的服务器IP:/var/www/interviewpilot/
```

---

## 第八步：配置 Nginx

### 创建配置文件

```bash
nano /etc/nginx/conf.d/interviewpilot.conf
```

### 写入以下内容

```nginx
server {
    listen 80;
    server_name 你的域名;   # 例如：interview.example.com

    # 网站根目录
    root /var/www/interviewpilot;
    index index.html;

    # 字符编码
    charset utf-8;

    # SPA 路由支持（非常重要，否则页面刷新 404）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }

    # 日志
    access_log /var/log/nginx/interviewpilot_access.log;
    error_log /var/log/nginx/interviewpilot_error.log;
}
```

> 将 `你的域名` 替换为实际域名（如 `interview.example.com`）
> 如果还没有域名，先用服务器 IP 测试，改为 `server_name 服务器IP;`

### 检查配置并重载

```bash
# 检查语法
nginx -t

# 重载配置
systemctl reload nginx
```

---

## 第九步：解析域名

登录你的域名服务商（阿里云 / 腾讯云 / Cloudflare 等），添加 DNS 记录：

| 记录类型 | 主机记录 | 记录值 |
|----------|----------|--------|
| A | @ | 你的服务器IP |
| A | www | 你的服务器IP |

> 域名解析通常 5 分钟~24 小时生效
> 可以用 `ping 你的域名` 检查是否解析成功

---

## 第十步：申请 SSL 证书（HTTPS）

```bash
# 安装 Certbot
apt install -y certbot python3-certbot-nginx

# 自动申请并配置 SSL（根据提示操作）
certbot --nginx -d 你的域名
```

> 输入邮箱（用于证书过期提醒）
> 同意服务条款，输入 `y`
> 选择是否重定向 HTTP 到 HTTPS，建议选 **2（重定向）**

申请成功后显示：

```
Congratulations! You have successfully enabled HTTPS on ...
```

---

## 第十一步：自动续期 SSL 证书

Let's Encrypt 证书有效期 90 天，Certbot 会自动配置定时任务，但最好验证一下：

```bash
# 查看定时任务
systemctl list-timers | grep certbot

# 测试自动续期（模拟，不真正续期）
certbot renew --dry-run
```

看到 `Congratulations, all renewals succeeded` 即表示定时续期正常。

---

## 第十二步：验证部署成功

在浏览器访问：

```
https://你的域名
```

或直接用服务器 IP（仅限测试）：

```
http://服务器IP
```

---

## 第十三步：设置开机自启（防止服务器重启后网站挂掉）

```bash
# 确保 nginx 开机自启
systemctl enable nginx

# 检查开机自启状态
systemctl is-enabled nginx
```

---

## 常用维护命令

```bash
# 重启网站
systemctl restart nginx

# 重载配置（不中断服务）
systemctl reload nginx

# 查看网站访问日志
tail -f /var/log/nginx/interviewpilot_access.log

# 查看网站错误日志
tail -f /var/log/nginx/interviewpilot_error.log

# 查看 nginx 进程状态
systemctl status nginx

# 更新网站（重新上传 dist 后执行）
systemctl reload nginx
```

---

## 完整部署流程汇总（简化版）

```bash
# ========== 服务器端（一次性执行）==========
apt update && apt upgrade -y
apt install -y curl wget git unzip zip nginx certbot python3-certbot-nginx
ufw allow 22/tcp && ufw allow 80/tcp && ufw allow 443/tcp && ufw enable
mkdir -p /var/www/interviewpilot && chmod -R 755 /var/www/interviewpilot

# ========== 服务器端：配置 Nginx ==========
cat > /etc/nginx/conf.d/interviewpilot.conf << 'EOF'
server {
    listen 80;
    server_name 你的域名;

    root /var/www/interviewpilot;
    index index.html;
    charset utf-8;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location ~ /\. {
        deny all;
    }

    access_log /var/log/nginx/interviewpilot_access.log;
    error_log /var/log/nginx/interviewpilot_error.log;
}
EOF

nginx -t && systemctl reload nginx

# ========== 服务器端：申请 SSL ==========
certbot --nginx -d 你的域名

# ========== 本地 Mac 端执行 ==========
# 在项目目录运行：
npm run build
scp -r dist/* root@你的服务器IP:/var/www/interviewpilot/
```

---

## 常见问题排查

### 1. 访问显示 403 Forbidden

```bash
# 检查文件权限
ls -la /var/www/interviewpilot/

# 确保 nginx 有读取权限
chmod -R 755 /var/www/interviewpilot
chown -R www-data:www-data /var/www/interviewpilot
```

### 2. 访问显示 502 Bad Gateway

```bash
# nginx 没启动
systemctl start nginx
systemctl status nginx
```

### 3. 页面刷新 404

检查 nginx 配置里是否有：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

这行必须有，否则 SPA 路由刷新会 404。

### 4. 域名解析不生效

- DNS 记录添加后需要等待生效（几分钟到48小时）
- 用 `ping 你的域名` 或 `nslookup 你的域名` 检查
- 国内服务器域名需要备案才能使用 80/443 端口

### 5. SSL 证书申请失败

- 确保域名已解析到服务器 IP
- 确保 80 和 443 端口已开放
- 云服务器检查安全组规则

---

## 后续更新网站

本地修改代码后：

```bash
# 本地重新打包
npm run build

# 上传到服务器
scp -r dist/* root@你的服务器IP:/var/www/interviewpilot/
```

Nginx 会自动加载新文件，无需重启。

---

> 开发者联系：[@qiqi_dawang](https://t.me/qiqi_dawang)
